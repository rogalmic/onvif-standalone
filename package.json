{
  "name": "onvif-standalone",
  "version": "0.0.1",
  "description": "Onvif standalone viewer based on sample web app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --windows --linux"
  },
  "repository": "https://github.com/rogalmic/onvif-standalone.git",
  "keywords": [
    "onvif",
    "camera",
    "standalone"
  ],
  "engines": {
    "node": "^10.0.0"
  },
  "author": "Michal Rogalinski",
  "license": "MIT",
  "dependencies": {
    "node-onvif": "^0.1.4",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "electron": "^2.0.0",
    "electron-builder": "^20.11.1"
  }
}
