```bash
# Clone this repository
git clone https://github.com/rogalmic/onvif-standalone.git
# Go into the repository
cd onvif-standalone
# Install dependencies
npm install
# Run the app
npm start
# Package the app
npm run-script build